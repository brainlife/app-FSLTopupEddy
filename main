#!/bin/bash
#PBS -l nodes=1:ppn=8,walltime=16:00:00
#PBS -N topup_eddy
#PBS -l vmem=16gb
#PBS -V

time singularity exec -e docker://brainlife/fsl:5.0.9 ./topup_eddy.sh

#if [ -s dwi.nii.gz ];
#then
#	echo 0 > finished
#else
#	echo "output missing"
#	echo 1 > finished
#	exit 1
#fi
